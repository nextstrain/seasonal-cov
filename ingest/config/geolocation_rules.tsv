# TSV file of geolocation rules with the format:
#
# '<raw_geolocation><tab><annotated_geolocation>' where the raw and
# annotated geolocations are formatted as
# '<region>/<country>/<division>/<location>'.
#
# When creating a general rule, the raw field value should be '*'
#
# Lines starting with '#' and content after trailing '#' will be
# ignored as comments.
